内联块标签
        内联块标签
            特性：
               1.同属性的标签排在同一排
               2.内容撑开宽度
               3。支持所有的css的样式
               4。支持换行被解析，间距大小取决于父级的font-size大小
        img标签
            写法：<img src=“图片路径地址” alt=“图片说明”></img>
              提示；
                路径地址一定要写
                alt属性一定要写
